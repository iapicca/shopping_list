import 'package:flutter/material.dart';

/// text shown when no items
const noItemYet = Center(
  child: Text(
    'no item yet',
    style: TextStyle(color: Colors.grey),
    textScaleFactor: 1.5,
  ),
);
